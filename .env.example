# 公開URL（Astro のビルド時に canonical / RSS / sitemap に使われる）
# 未設定なら astro.config.mjs の既定値が使われる
SITE_URL=

# ── Cloudflare（wrangler deploy をローカルから叩く場合。CIでは Secrets） ──
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
